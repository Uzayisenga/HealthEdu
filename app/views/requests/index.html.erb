if current_user.userrole=”instructor”
    @request.all.each do |request|
    
    request.user.email
    
    
    
    request.course.user.email
    
    <end>
    <div class="container"
<p id="notice"><%= notice %></p>

<h1>Payments</h1>

<table>
  <thead>
    <tr>
    <th>Course</th>
      <th>User</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <%= requests.each do |request| %>
    
      <tr>
        <td><%= request.course.content %></td>
        <td><%= payment.user.email %></td>
        <td><%= link_to 'Show', request %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'pay', new_payment_path %>
</dev>
